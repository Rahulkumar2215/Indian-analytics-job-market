# --- secrets: never commit API keys ---
.env
*.env
secrets.*

# --- raw API data (Adzuna content; regenerate with 01_data_extraction.py) ---
raw_job_postings.csv

# --- saved models (large, regenerable by running the notebook) ---
*.pkl

# --- generated CSVs written to the repo root when the notebook runs ---
# (copy the ones you want to show into data/ instead)
/cleaned_job_postings.csv
/powerbi_dataset.csv
/skill_salary_summary.csv
/cluster_profiles.csv

# --- Python / Jupyter ---
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# --- Power BI temp / OS files ---
~$*
*.pbix.bak
.DS_Store
Thumbs.db
desktop.ini
